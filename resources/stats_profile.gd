class_name StatsProfile extends Resource
## StatsProfile — base stat bundle referenced by MinionDefinition and
## AdventurerDefinition. Keeps stat data editable without touching scripts.

@export var max_hp: float = 10.0
@export var attack: float = 2.0
@export var defense: float = 0.0
@export var move_speed: float = 3.0
